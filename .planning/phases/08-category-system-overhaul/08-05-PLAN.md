---
phase: 08-category-system-overhaul
plan: 05
type: execute
wave: 4
depends_on: [08-03, 08-04]
files_modified:
  - lib/features/transactions/presentation/screens/add_transaction_screen.dart
  - lib/features/budgets/presentation/screens/set_budget_screen.dart
  - lib/features/recurring/presentation/screens/add_recurring_screen.dart
autonomous: false
requirements: [D-02, D-25, D-26]

must_haves:
  truths:
    - "add_transaction_screen.dart uses shared CategoryPickerSheet (not inline _CategoryPickerSheet)"
    - "set_budget_screen.dart uses shared CategoryPickerSheet (not inline picker)"
    - "add_recurring_screen.dart uses shared CategoryPickerSheet (not inline picker)"
    - "Each file is modified ONE AT A TIME with verification between each (D-25)"
    - "add_transaction_screen.dart modification is ONLY lines 1608-1763 removal + import (D-26)"
  artifacts:
    - path: "lib/features/transactions/presentation/screens/add_transaction_screen.dart"
      provides: "Uses shared CategoryPickerSheet, _CategoryPickerSheet class removed"
    - path: "lib/features/budgets/presentation/screens/set_budget_screen.dart"
      provides: "Uses shared CategoryPickerSheet instead of inline picker"
    - path: "lib/features/recurring/presentation/screens/add_recurring_screen.dart"
      provides: "Uses shared CategoryPickerSheet instead of inline picker"
  key_links:
    - from: "lib/features/transactions/presentation/screens/add_transaction_screen.dart"
      to: "lib/shared/widgets/pickers/category_picker_sheet.dart"
      via: "import + showModalBottomSheet call"
      pattern: "CategoryPickerSheet"
    - from: "lib/features/budgets/presentation/screens/set_budget_screen.dart"
      to: "lib/shared/widgets/pickers/category_picker_sheet.dart"
      via: "import + showModalBottomSheet call"
      pattern: "CategoryPickerSheet"
    - from: "lib/features/recurring/presentation/screens/add_recurring_screen.dart"
      to: "lib/shared/widgets/pickers/category_picker_sheet.dart"
      via: "import + showModalBottomSheet call"
      pattern: "CategoryPickerSheet"
---

<objective>
Integrate the shared CategoryPickerSheet into all 3 consumer screens, one file at a time.

Purpose: Replace 3 inline picker implementations with the single shared widget (D-02). Per D-25, each file is modified independently with verification between. Per D-26, add_transaction_screen.dart is the highest risk file (16 features) — ONLY the picker extraction is allowed.
Output: All 3 screens use the shared CategoryPickerSheet. Zero feature regressions.
</objective>

<execution_context>
@D:/Masarify-Plus/.claude/get-shit-done/workflows/execute-plan.md
@D:/Masarify-Plus/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-category-system-overhaul/08-CONTEXT.md
@.planning/phases/08-category-system-overhaul/08-RESEARCH.md
@.planning/phases/08-category-system-overhaul/08-01-SUMMARY.md
@.planning/phases/08-category-system-overhaul/08-03-SUMMARY.md

<interfaces>
<!-- Shared picker (from Plan 03) -->
From lib/shared/widgets/pickers/category_picker_sheet.dart:
```dart
class CategoryPickerSheet extends StatefulWidget {
  const CategoryPickerSheet({
    super.key,
    required this.categories,
    required this.onSelected,
    this.selectedId,
  });
  final List<CategoryEntity> categories;
  final int? selectedId;
  final ValueChanged<int> onSelected;
}
```

<!-- Current inline picker in add_transaction_screen.dart (lines 1608-1763) -->
```dart
class _CategoryPickerSheet extends StatefulWidget {
  const _CategoryPickerSheet({
    required this.categories,
    required this.selectedId,
    required this.onSelected,
  });
  final List<CategoryEntity> categories;
  final int? selectedId;
  final ValueChanged<int> onSelected;
}
// ... 155 lines of implementation
```

<!-- Call sites using the picker: -->
// add_transaction_screen.dart uses showModalBottomSheet with _CategoryPickerSheet
// set_budget_screen.dart has inline picker (lines ~69-119)
// add_recurring_screen.dart has inline picker (lines ~163-200)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace picker in add_transaction_screen.dart (HIGHEST RISK — D-26)</name>
  <files>lib/features/transactions/presentation/screens/add_transaction_screen.dart</files>
  <read_first>
    - lib/features/transactions/presentation/screens/add_transaction_screen.dart (READ THE ENTIRE FILE — all 1763 lines)
    - lib/shared/widgets/pickers/category_picker_sheet.dart (the shared widget to use)
  </read_first>
  <action>
**Per D-26: This is the HIGHEST RISK file (1763 lines, 16 features). The ONLY allowed changes are:**

1. **Add import** at the top of the file:
   ```dart
   import '../../../../shared/widgets/pickers/category_picker_sheet.dart';
   ```

2. **Delete lines 1608-1763** (the entire `_CategoryPickerSheet` class and its `_CategoryPickerSheetState`). These are the last ~155 lines of the file, after the closing brace of the main screen widget.

3. **Update every `showModalBottomSheet` call** that references `_CategoryPickerSheet` to use `CategoryPickerSheet` (public name, same parameters).

   Find calls like:
   ```dart
   showModalBottomSheet(
     context: context,
     isScrollControlled: true,
     useSafeArea: true,
     builder: (_) => _CategoryPickerSheet(
       categories: categories,
       selectedId: _selectedCategoryId,
       onSelected: (id) { ... },
     ),
   );
   ```

   Change `_CategoryPickerSheet` to `CategoryPickerSheet` in each call. The parameter names (`categories`, `selectedId`, `onSelected`) are identical between the old private and new public widget, so NO other changes are needed.

4. **NOTHING ELSE changes.** Do not touch:
   - The mixin declarations
   - The `_save()` method
   - The wallet picker code
   - The location/goal code
   - The category suggestion/smart defaults code
   - The voice input integration
   - The bottom sheet variant (AddTransactionSheet)
   - Any import other than the one being added
   - Any line outside of the picker class definition and its call sites

**Verification after this task:**
Run `flutter analyze lib/features/transactions/` to confirm zero issues.
Then mentally verify (the executor should grep/check):
- `_CategoryPickerSheet` no longer exists in the file
- `CategoryPickerSheet` is imported and used
- Total line count dropped by ~155 lines
- All other code is byte-identical to before (except the import and call site name changes)
  </action>
  <verify>
    <automated>flutter analyze lib/features/transactions/ && flutter test</automated>
  </verify>
  <acceptance_criteria>
    - lib/features/transactions/presentation/screens/add_transaction_screen.dart does NOT contain `class _CategoryPickerSheet`
    - lib/features/transactions/presentation/screens/add_transaction_screen.dart does NOT contain `class _CategoryPickerSheetState`
    - File contains `import '../../../../shared/widgets/pickers/category_picker_sheet.dart'`
    - File contains `CategoryPickerSheet(` (public name) in showModalBottomSheet calls
    - File still contains the mixin declarations (unchanged)
    - File still contains `_save()` method (unchanged)
    - File still contains wallet picker code (unchanged)
    - `flutter analyze lib/` exits with "No issues found"
    - `flutter test` — all existing tests pass
    - Feature Preservation Checklist (FULL for this file per D-24):
      - [ ] Manual expense/income creation works
      - [ ] Cash withdrawal/deposit creates Transfer
      - [ ] Edit transaction loads correctly
      - [ ] Title -> category auto-suggestion fires
      - [ ] Smart default: last-used category pre-selected
      - [ ] Category chips show frequency-sorted categories
      - [ ] "More" opens full search picker with working search
      - [ ] Wallet picker respects selectedAccountIdProvider
      - [ ] Category learning recorded on save (recordMapping)
      - [ ] Bottom sheet variant (AddTransactionSheet) works
  </acceptance_criteria>
  <done>add_transaction_screen.dart now uses shared CategoryPickerSheet. Private _CategoryPickerSheet class deleted (~155 lines). All 16 features preserved.</done>
</task>

<task type="auto">
  <name>Task 2: Replace picker in set_budget_screen.dart and add_recurring_screen.dart</name>
  <files>
    lib/features/budgets/presentation/screens/set_budget_screen.dart,
    lib/features/recurring/presentation/screens/add_recurring_screen.dart
  </files>
  <read_first>
    - lib/features/budgets/presentation/screens/set_budget_screen.dart (ENTIRE file)
    - lib/features/recurring/presentation/screens/add_recurring_screen.dart (ENTIRE file)
    - lib/shared/widgets/pickers/category_picker_sheet.dart (the shared widget)
  </read_first>
  <action>
**set_budget_screen.dart — Replace inline picker (per D-25, modify one file then verify):**

1. Read the entire file to locate the inline category picker code (~lines 69-119 based on CONTEXT.md).
2. Add import: `import '../../../../shared/widgets/pickers/category_picker_sheet.dart';`
3. Find the inline picker implementation (likely a simple GridView or ListView for category selection built directly in the build method).
4. Replace the inline picker invocation with a `showModalBottomSheet` call using `CategoryPickerSheet`:
   ```dart
   showModalBottomSheet(
     context: context,
     isScrollControlled: true,
     useSafeArea: true,
     builder: (_) => CategoryPickerSheet(
       categories: categories, // from the provider
       selectedId: _selectedCategoryId,
       onSelected: (id) {
         setState(() => _selectedCategoryId = id);
         context.pop();
       },
     ),
   );
   ```
5. The category display in the form should show a tappable tile/card that opens the picker sheet on tap, showing the selected category name + GlassCategoryIcon.
6. Run `flutter analyze lib/features/budgets/` to verify zero issues.

**add_recurring_screen.dart — Replace inline picker:**

1. Read the entire file to locate the inline category picker code (~lines 163-200 based on CONTEXT.md).
2. Add import: `import '../../../../shared/widgets/pickers/category_picker_sheet.dart';`
3. Replace the inline picker with the same `showModalBottomSheet` + `CategoryPickerSheet` pattern as above.
4. Run `flutter analyze lib/features/recurring/` to verify zero issues.

**For both files:** The existing category display (how the selected category is shown in the form) should be preserved — only the PICKER MECHANISM changes. If the form currently shows the category name inline and opens a dialog/sheet on tap, keep that pattern but point to CategoryPickerSheet.

**Per D-25: Modify set_budget_screen FIRST, verify it compiles, then modify add_recurring_screen.**
  </action>
  <verify>
    <automated>flutter analyze lib/features/budgets/ lib/features/recurring/ && flutter test</automated>
  </verify>
  <acceptance_criteria>
    - lib/features/budgets/presentation/screens/set_budget_screen.dart contains `import` for category_picker_sheet.dart
    - lib/features/budgets/presentation/screens/set_budget_screen.dart contains `CategoryPickerSheet(` usage
    - lib/features/recurring/presentation/screens/add_recurring_screen.dart contains `import` for category_picker_sheet.dart
    - lib/features/recurring/presentation/screens/add_recurring_screen.dart contains `CategoryPickerSheet(` usage
    - set_budget_screen.dart does NOT contain `Navigator.pop` (use `context.pop()` instead)
    - add_recurring_screen.dart does NOT contain `Navigator.pop` (use `context.pop()` instead)
    - `flutter analyze lib/` exits with "No issues found"
    - `flutter test` — all tests pass
    - Feature Preservation Checklist:
      - [ ] Budget creation still works with category selection
      - [ ] Budget edit loads existing category
      - [ ] Recurring/subscription creation still works with category selection
      - [ ] All other form fields (amount, wallet, frequency) unchanged
  </acceptance_criteria>
  <done>Both set_budget_screen and add_recurring_screen now use shared CategoryPickerSheet. All 3 inline pickers unified into one shared widget.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human verification of shared picker across all 3 screens</name>
  <action>Verify that the shared CategoryPickerSheet works correctly in all 3 consumer screens (add_transaction, set_budget, add_recurring) by running the app and testing each picker invocation point. This is the highest-risk integration point of the entire phase.</action>
  <what-built>Shared CategoryPickerSheet integrated into all 3 screens (add_transaction, set_budget, add_recurring). This is the highest-risk integration point of the entire phase.</what-built>
  <how-to-verify>
    1. Open the app and navigate to Add Transaction (tap FAB)
    2. Tap the category field — verify the picker opens as a bottom sheet with "Most Used" section and "All Categories" grouped by type
    3. Use the search bar — verify it filters categories in real-time
    4. Select a category — verify it closes the picker and shows the selected category
    5. Save the transaction — verify it saves correctly
    6. Navigate to Planning > Budgets > Set Budget
    7. Tap category picker — verify same picker sheet appears
    8. Navigate to Subscriptions > Add
    9. Tap category picker — verify same picker sheet appears
    10. Switch to Arabic — verify all 3 pickers work in RTL
    11. Verify the "Most Used" section shows categories you've actually used (if any transactions exist)
  </how-to-verify>
  <verify>Human approval required</verify>
  <done>User has visually confirmed all 3 pickers use the shared CategoryPickerSheet with correct UX in both LTR and RTL.</done>
  <resume-signal>Type "approved" or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
1. `flutter analyze lib/` ��� zero issues
2. `flutter test` — all tests pass
3. All 3 screens use CategoryPickerSheet (shared widget)
4. No inline picker code remains in any of the 3 files
5. Human verification of picker UX across all 3 screens
</verification>

<success_criteria>
- All 3 inline picker implementations replaced with shared CategoryPickerSheet
- add_transaction_screen.dart reduced by ~155 lines (picker class deleted)
- Zero regressions in transaction creation, budget creation, recurring creation
- Picker shows Most Used + grouped All Categories in all 3 screens
- Human approval after visual verification
</success_criteria>

<output>
After completion, create `.planning/phases/08-category-system-overhaul/08-05-SUMMARY.md`
</output>
