---
phase: 08-category-system-overhaul
plan: 07
type: execute
wave: 6
depends_on: [08-01, 08-06]
files_modified:
  - lib/features/dashboard/presentation/widgets/filter_bar.dart
  - lib/shared/providers/smart_defaults_provider.dart
  - lib/core/services/category_frequency_service.dart
  - lib/data/services/backup_service_impl.dart
autonomous: true
requirements: [D-13, D-17, D-23, D-24]

must_haves:
  truths:
    - "Dashboard filter_bar reads top categories from categoriesProvider (DB usageCount) not SharedPreferences"
    - "smart_defaults_provider reads from categoriesProvider (DB usageCount) not categoryFrequencyServiceProvider"
    - "CategoryFrequencyService is deprecated or deleted — single source of truth is DB usageCount"
    - "backup_service_impl handles groupType removal gracefully (null on export, ignore on import)"
  artifacts:
    - path: "lib/features/dashboard/presentation/widgets/filter_bar.dart"
      provides: "Top category chips from DB usageCount"
      contains: "categoriesProvider"
    - path: "lib/shared/providers/smart_defaults_provider.dart"
      provides: "Smart defaults from DB usageCount"
    - path: "lib/data/services/backup_service_impl.dart"
      provides: "Backward-compatible backup with nullable groupType"
  key_links:
    - from: "lib/features/dashboard/presentation/widgets/filter_bar.dart"
      to: "lib/shared/providers/category_provider.dart"
      via: "ref.watch(categoriesProvider) or ref.watch(topUsedCategoriesProvider)"
      pattern: "topUsedCategoriesProvider|categoriesProvider"
    - from: "lib/shared/providers/smart_defaults_provider.dart"
      to: "lib/shared/providers/category_provider.dart"
      via: "ref.watch(categoriesProvider)"
      pattern: "categoriesProvider"
---

<objective>
Migrate filter_bar and smart_defaults from SharedPreferences-based frequency to DB usageCount, and remove the old CategoryFrequencyService.

Purpose: Per D-13, usage count should be the single source of truth (DB column, not SharedPreferences). Per Pitfall 4, having two sources of truth causes inconsistent "top categories" between picker and dashboard. This plan eliminates the duplication.
Output: All frequency consumers read from DB. CategoryFrequencyService removed. Backup handles groupType.
</objective>

<execution_context>
@D:/Masarify-Plus/.claude/get-shit-done/workflows/execute-plan.md
@D:/Masarify-Plus/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-category-system-overhaul/08-CONTEXT.md
@.planning/phases/08-category-system-overhaul/08-RESEARCH.md
@.planning/phases/08-category-system-overhaul/08-01-SUMMARY.md
@.planning/phases/08-category-system-overhaul/08-06-SUMMARY.md

<interfaces>
<!-- Providers (after Plan 01) -->
From lib/shared/providers/category_provider.dart:
```dart
final categoriesProvider = StreamProvider<List<CategoryEntity>>(...);
final topUsedCategoriesProvider = Provider<List<CategoryEntity>>(...);
```

<!-- CategoryFrequencyService (to be removed) -->
From lib/core/services/category_frequency_service.dart:
```dart
class CategoryFrequencyService {
  // SharedPreferences-based tracking
  Future<void> recordUsage(int categoryId);
  Future<List<int>> getTopCategoryIds({int limit = 5});
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate filter_bar and smart_defaults to DB usageCount</name>
  <files>
    lib/features/dashboard/presentation/widgets/filter_bar.dart,
    lib/shared/providers/smart_defaults_provider.dart
  </files>
  <read_first>
    - lib/features/dashboard/presentation/widgets/filter_bar.dart (ENTIRE file — understand how it currently uses categoryFrequencyServiceProvider)
    - lib/shared/providers/smart_defaults_provider.dart (ENTIRE file — understand current provider source)
    - lib/core/services/category_frequency_service.dart (understand what it provides)
    - lib/shared/providers/category_provider.dart (topUsedCategoriesProvider)
  </read_first>
  <action>
**filter_bar.dart — Switch from SharedPreferences to DB provider:**

1. Find where the file references `categoryFrequencyServiceProvider` (or `CategoryFrequencyService`).
2. Replace the frequency-based category lookup with `topUsedCategoriesProvider`:
   - Old: Read top category IDs from SharedPreferences, then look up categories by ID
   - New: `final topCategories = ref.watch(topUsedCategoriesProvider);`
3. Remove the import of `categoryFrequencyServiceProvider` and/or `CategoryFrequencyService`.
4. Add import of `category_provider.dart` if not already present.
5. The rest of the filter_bar logic (chip rendering, selection, filtering) should remain the same — only the DATA SOURCE changes.

**smart_defaults_provider.dart — Switch to DB provider:**

1. Find where it references `categoryFrequencyServiceProvider`.
2. Replace with a derived computation from `categoriesProvider`:
   ```dart
   final smartDefaultCategoryProvider = Provider<CategoryEntity?>((ref) {
     final categories = ref.watch(categoriesProvider).valueOrNull ?? [];
     if (categories.isEmpty) return null;
     // Return the most-used category (highest usageCount)
     final sorted = [...categories]..sort((a, b) => b.usageCount.compareTo(a.usageCount));
     return sorted.first.usageCount > 0 ? sorted.first : null;
   });
   ```
3. Remove the import of `CategoryFrequencyService`/`categoryFrequencyServiceProvider`.

**Also find and remove ALL other references** to `categoryFrequencyServiceProvider` or `CategoryFrequencyService` in the codebase. The executor should grep:
```bash
grep -r "categoryFrequencyService\|CategoryFrequencyService" lib/ --include="*.dart"
```
And update each consumer to use the DB-backed provider instead.
  </action>
  <verify>
    <automated>flutter analyze lib/ && flutter test</automated>
  </verify>
  <acceptance_criteria>
    - lib/features/dashboard/presentation/widgets/filter_bar.dart contains `topUsedCategoriesProvider` or `categoriesProvider` (not `categoryFrequencyServiceProvider`)
    - lib/features/dashboard/presentation/widgets/filter_bar.dart does NOT contain `CategoryFrequencyService`
    - lib/shared/providers/smart_defaults_provider.dart does NOT contain `CategoryFrequencyService`
    - `grep -r "categoryFrequencyServiceProvider" lib/ --include="*.dart"` returns zero results (or only the service definition file itself)
    - `flutter analyze lib/` exits with "No issues found"
    - `flutter test` — all tests pass
    - Feature Preservation Checklist:
      - [ ] Dashboard filter chips show top categories
      - [ ] Smart default category pre-selected in add transaction
      - [ ] Cash wallet visible in home chips
  </acceptance_criteria>
  <done>Dashboard filter_bar and smart_defaults now read from DB usageCount (single source of truth). SharedPreferences-based frequency tracking no longer consumed.</done>
</task>

<task type="auto">
  <name>Task 2: Remove CategoryFrequencyService + fix backup groupType compat</name>
  <files>
    lib/core/services/category_frequency_service.dart,
    lib/data/services/backup_service_impl.dart
  </files>
  <read_first>
    - lib/core/services/category_frequency_service.dart (ENTIRE file — confirm no remaining consumers)
    - lib/data/services/backup_service_impl.dart (lines ~465-475 for export, lines ~630-645 for import — groupType references)
    - grep output for CategoryFrequencyService across lib/ (confirm Task 1 removed all consumers)
  </read_first>
  <action>
**Step 1: Verify no remaining consumers of CategoryFrequencyService.**
Run: `grep -r "CategoryFrequencyService\|categoryFrequencyService" lib/ --include="*.dart" -l`
The only file that should remain is `category_frequency_service.dart` itself. If any other file still references it, fix those references first (switch to categoriesProvider/topUsedCategoriesProvider).

**Step 2: Delete lib/core/services/category_frequency_service.dart.**
This file is now fully superseded by the DB `usageCount` column. Delete it entirely.

Also check if there's a provider registration for it (like `categoryFrequencyServiceProvider`) in a provider file. If so, delete that provider definition too.

**Step 3: Fix backup_service_impl.dart for groupType backward compatibility (Pitfall 3).**

In the **export** section (~line 471):
The line `'groupType': c.groupType` will cause a compile error since entity no longer has `groupType`. Change to:
```dart
'groupType': null, // Preserved for backward compatibility with older backups
```

In the **import** section (~line 639):
The line `groupType: Value(m['groupType'] as String?)` will cause a compile error since CategoriesCompanion no longer has `groupType` field. Remove this line entirely. The physical SQLite column still exists (orphan column) so existing data is safe, but Drift no longer generates the field.

Also check if there's a `usageCount` field that needs to be handled in backup:
- **Export:** Add `'usageCount': c.usageCount` (read from entity field)
- **Import:** The CategoriesCompanion may not need usageCount on import since backfill happens from transaction count in migration. But if restoring a backup should preserve usage counts, add it: check if CategoriesCompanion accepts `usageCount` and if so, include it.

**Step 4: Run `dart run build_runner build --delete-conflicting-outputs`** if the Drift table changes require regeneration.

**Step 5: Verify `flutter analyze lib/` passes.**
  </action>
  <verify>
    <automated>flutter analyze lib/ && flutter test</automated>
  </verify>
  <acceptance_criteria>
    - lib/core/services/category_frequency_service.dart does NOT exist (deleted)
    - `grep -r "CategoryFrequencyService" lib/ --include="*.dart"` returns zero results
    - `grep -r "categoryFrequencyServiceProvider" lib/ --include="*.dart"` returns zero results
    - lib/data/services/backup_service_impl.dart export section contains `'groupType': null`
    - lib/data/services/backup_service_impl.dart import section does NOT contain `groupType: Value(`
    - `flutter analyze lib/` exits with "No issues found"
    - `flutter test` — all tests pass
    - Feature Preservation Checklist:
      - [ ] Backup export still works (test manually or via test)
      - [ ] App launches without crash (no missing provider error)
  </acceptance_criteria>
  <done>CategoryFrequencyService deleted (superseded by DB usageCount). Backup service handles groupType removal gracefully. Single source of truth for category frequency.</done>
</task>

</tasks>

<verification>
1. `flutter analyze lib/` — zero issues
2. `flutter test` — all tests pass
3. CategoryFrequencyService file deleted
4. No remaining references to CategoryFrequencyService in codebase
5. Dashboard filter chips work with DB-backed frequency
6. Backup export/import handles groupType gracefully
</verification>

<success_criteria>
- Dashboard filter chips show top categories from DB usageCount (not SharedPreferences)
- Smart default category works from DB usageCount
- CategoryFrequencyService fully removed — zero references in codebase
- Backup export writes null for groupType; import ignores it
- Zero analyzer issues, all tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/08-category-system-overhaul/08-07-SUMMARY.md`
</output>
