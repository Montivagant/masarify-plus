---
phase: 08-category-system-overhaul
plan: 06
type: execute
wave: 5
depends_on: [08-01, 08-05]
files_modified:
  - lib/features/transactions/presentation/screens/add_transaction_screen.dart
  - lib/features/voice_input/presentation/screens/voice_confirm_screen.dart
  - lib/core/services/ai/chat_action_executor.dart
  - lib/data/database/daos/category_dao.dart
autonomous: true
requirements: [D-12, D-18, D-19, D-20, D-22]

must_haves:
  truths:
    - "usageCount incremented on every transaction save from manual path (add_transaction_screen)"
    - "usageCount incremented on every voice confirm save (voice_confirm_screen)"
    - "usageCount incremented on every AI chat transaction creation (chat_action_executor)"
    - "AI chat handles deleted/merged category gracefully with available list (D-19)"
    - "All 3 save paths call repository.incrementUsage(categoryId) after successful save"
  artifacts:
    - path: "lib/features/transactions/presentation/screens/add_transaction_screen.dart"
      provides: "Manual save path increments usageCount"
      contains: "incrementUsage"
    - path: "lib/features/voice_input/presentation/screens/voice_confirm_screen.dart"
      provides: "Voice save path increments usageCount"
      contains: "incrementUsage"
    - path: "lib/core/services/ai/chat_action_executor.dart"
      provides: "AI chat save path increments usageCount + graceful category-not-found"
      contains: "incrementUsage"
  key_links:
    - from: "lib/features/transactions/presentation/screens/add_transaction_screen.dart"
      to: "lib/domain/repositories/i_category_repository.dart"
      via: "ref.read(categoryRepositoryProvider).incrementUsage(categoryId)"
      pattern: "incrementUsage"
    - from: "lib/features/voice_input/presentation/screens/voice_confirm_screen.dart"
      to: "lib/domain/repositories/i_category_repository.dart"
      via: "ref.read(categoryRepositoryProvider).incrementUsage(categoryId)"
      pattern: "incrementUsage"
    - from: "lib/core/services/ai/chat_action_executor.dart"
      to: "lib/domain/repositories/i_category_repository.dart"
      via: "categoryRepository.incrementUsage(categoryId)"
      pattern: "incrementUsage"
---

<objective>
Wire usageCount increment into all 3 transaction save paths and update AI integration for deleted/merged categories.

Purpose: Per D-12, usageCount must be incremented from ALL entry paths (manual, voice, AI chat). Per D-19, AI must handle deleted categories gracefully. These are HIGH-RISK file modifications that must be minimal and surgical.
Output: All save paths increment usageCount. AI category-not-found shows available list.
</objective>

<execution_context>
@D:/Masarify-Plus/.claude/get-shit-done/workflows/execute-plan.md
@D:/Masarify-Plus/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-category-system-overhaul/08-CONTEXT.md
@.planning/phases/08-category-system-overhaul/08-RESEARCH.md
@.planning/phases/08-category-system-overhaul/08-01-SUMMARY.md
@.planning/phases/08-category-system-overhaul/08-05-SUMMARY.md

<interfaces>
<!-- Repository interface (after Plan 01) -->
From lib/domain/repositories/i_category_repository.dart:
```dart
Future<void> incrementUsage(int categoryId);
```

<!-- Provider for repository -->
From lib/shared/providers/repository_providers.dart:
```dart
final categoryRepositoryProvider = Provider<ICategoryRepository>((ref) { ... });
```

<!-- Chat action executor interface -->
From lib/core/services/ai/chat_action_executor.dart:
```dart
class ChatActionExecutor {
  // _executeTransaction creates transactions from AI chat
  // Has access to category matching logic
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire usageCount increment into manual and voice save paths</name>
  <files>
    lib/features/transactions/presentation/screens/add_transaction_screen.dart,
    lib/features/voice_input/presentation/screens/voice_confirm_screen.dart
  </files>
  <read_first>
    - lib/features/transactions/presentation/screens/add_transaction_screen.dart (find the _save() method — locate where the transaction is inserted successfully, then add incrementUsage after it)
    - lib/features/voice_input/presentation/screens/voice_confirm_screen.dart (find the save/confirm method — locate where each draft's transaction is saved)
    - lib/shared/providers/repository_providers.dart (confirm categoryRepositoryProvider name)
  </read_first>
  <action>
**add_transaction_screen.dart — Add incrementUsage call in _save() method:**

Find the `_save()` method. After the transaction is successfully inserted (the line where the repository `create()` or `insert()` call returns), add:

```dart
// D-12: Increment category usage count
await ref.read(categoryRepositoryProvider).incrementUsage(_selectedCategoryId!);
```

Place this AFTER the successful transaction save but BEFORE the navigation (pop/success snackbar). The `_selectedCategoryId` should already exist as a state variable in the screen.

**Do NOT modify anything else in this file.** After Plan 05, the only changes should have been the picker extraction. This task adds ONE line (the incrementUsage call) inside the existing _save() method.

Also add the import for `categoryRepositoryProvider` if not already present (it may already be imported for other uses).

**voice_confirm_screen.dart — Add incrementUsage call in the confirm/save method:**

Find the method that saves each voice transaction draft (likely `_confirmAll()` or similar). For each draft that gets saved successfully, add:

```dart
// D-12: Increment category usage count
if (draft.categoryId != null) {
  await ref.read(categoryRepositoryProvider).incrementUsage(draft.categoryId!);
}
```

Place this AFTER each individual draft's transaction save succeeds.

**Do NOT modify anything else** in voice_confirm_screen.dart — no mixin changes, no UI changes, no wallet picker changes. Only the incrementUsage call in the save path.
  </action>
  <verify>
    <automated>flutter analyze lib/features/transactions/ lib/features/voice_input/ && flutter test</automated>
  </verify>
  <acceptance_criteria>
    - lib/features/transactions/presentation/screens/add_transaction_screen.dart contains `incrementUsage` call in _save() method
    - lib/features/voice_input/presentation/screens/voice_confirm_screen.dart contains `incrementUsage` call in save method
    - Both files import categoryRepositoryProvider (directly or transitively)
    - `flutter analyze lib/` exits with "No issues found"
    - `flutter test` — all tests pass
    - Feature Preservation Checklist (add_transaction_screen.dart):
      - [ ] Manual expense/income creation works
      - [ ] Cash withdrawal/deposit creates Transfer
      - [ ] Edit transaction loads correctly
      - [ ] Wallet picker respects selectedAccountIdProvider
      - [ ] Category learning recorded on save (recordMapping)
    - Feature Preservation Checklist (voice_confirm_screen.dart):
      - [ ] Category auto-match by iconName resolves
      - [ ] Category keyword fallback matching works
      - [ ] "Other" category fallback assigned when no match
      - [ ] Voice "cash" routes to system Cash wallet
      - [ ] Subscription detection suggestion appears
      - [ ] Category picker per draft works
  </acceptance_criteria>
  <done>Manual and voice save paths both increment usageCount after successful transaction save.</done>
</task>

<task type="auto">
  <name>Task 2: Wire usageCount in AI chat executor + graceful category-not-found (D-19)</name>
  <files>lib/core/services/ai/chat_action_executor.dart</files>
  <read_first>
    - lib/core/services/ai/chat_action_executor.dart (ENTIRE file — understand all action types, category matching, _executeTransaction method)
    - lib/core/services/ai/ai_chat_service.dart (understand how category list is injected into system prompt — D-18)
    - lib/shared/providers/category_provider.dart (categoriesProvider)
  </read_first>
  <action>
**chat_action_executor.dart — Two changes:**

**Change 1: Add incrementUsage call after transaction creation.**
Find the `_executeTransaction()` method (or equivalent method that creates transactions from AI chat actions). After the transaction is successfully saved, add:

```dart
// D-12: Increment category usage count
if (categoryId != null) {
  await _categoryRepository.incrementUsage(categoryId);
}
```

If the executor doesn't already have a reference to `ICategoryRepository`, add it as a constructor parameter or obtain it from the provider.

**Change 2: Graceful category-not-found handling (D-19).**
Find the category matching logic (where the AI's suggested category name is resolved to a category ID). Currently this likely does a fuzzy match against available categories. Add an error path:

If no matching category is found after fuzzy matching:
- Do NOT auto-fallback to "Other" category (per D-19)
- Instead, return an error message to the chat that includes the list of available categories:

```dart
if (matchedCategory == null) {
  // D-19: Show available categories and let AI re-suggest
  final availableNames = categories.map((c) => c.name).join(', ');
  return ChatActionResult.error(
    'Category "${suggestedCategory}" not found. Available categories: $availableNames',
  );
}
```

The exact implementation depends on how the executor returns results to the chat UI. Read the file carefully to understand the existing error handling pattern and follow it. The key requirement is: (a) no silent fallback to "Other", (b) available category list shown to user, (c) AI can re-suggest.

**Do NOT modify:** Other action types (budget, transfer, recurring), the message parsing logic, the system prompt construction (that's in ai_chat_service.dart and is already fine per D-18 since financialContextProvider uses live DB).
  </action>
  <verify>
    <automated>flutter analyze lib/core/services/ai/ && flutter test</automated>
  </verify>
  <acceptance_criteria>
    - lib/core/services/ai/chat_action_executor.dart contains `incrementUsage` call after transaction save
    - File contains category-not-found error handling that includes available category names
    - File does NOT fallback to "Other" category when match fails (D-19)
    - `flutter analyze lib/` exits with "No issues found"
    - `flutter test` — all tests pass
    - Feature Preservation Checklist (AI Chat):
      - [ ] AI creates transactions with correct category
      - [ ] AI creates budgets with correct category
      - [ ] AI creates recurring rules with correct category
      - [ ] Category not found shows available categories (new behavior)
      - [ ] Subscription suggestion after transaction creation
  </acceptance_criteria>
  <done>AI chat executor increments usageCount and handles deleted/merged categories gracefully by showing available category list instead of silent "Other" fallback.</done>
</task>

</tasks>

<verification>
1. `flutter analyze lib/` — zero issues
2. `flutter test` — all tests pass
3. All 3 save paths (manual, voice, AI) increment usageCount
4. AI category-not-found shows available list (no "Other" fallback)
</verification>

<success_criteria>
- Creating a transaction manually increments the category's usageCount
- Confirming a voice draft increments the category's usageCount
- AI chat transaction creation increments the category's usageCount
- AI suggesting a deleted category triggers graceful error with available list
- Zero regressions in any save path
</success_criteria>

<output>
After completion, create `.planning/phases/08-category-system-overhaul/08-06-SUMMARY.md`
</output>
