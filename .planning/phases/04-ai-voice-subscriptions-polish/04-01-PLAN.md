---
phase: 04-ai-voice-subscriptions-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/l10n/app_en.arb
  - lib/l10n/app_ar.arb
  - lib/core/services/ai/ai_chat_service.dart
requirements: [AI-01, AI-02, AI-06]
autonomous: true
must_haves:
  truths:
    - "App compiles with zero analyzer errors on chat_screen.dart"
    - "AI responds entirely in Arabic when locale is Arabic — no mixed-language sentences"
    - "AI system prompt includes current date/time for both EN and AR locales"
    - "AI correctly routes transfer phrases like 'حولت من CIB لـ NBE' to create_transfer action"
  artifacts:
    - path: "lib/l10n/app_en.arb"
      provides: "Missing l10n keys for chat actions"
      contains: "chat_action_wallet_not_found"
    - path: "lib/l10n/app_ar.arb"
      provides: "Arabic translations for missing chat action keys"
      contains: "chat_action_wallet_not_found"
    - path: "lib/core/services/ai/ai_chat_service.dart"
      provides: "Expanded transfer keywords in EN and AR prompts, fixed AR budget date bug"
      contains: "سددت"
  key_links:
    - from: "lib/features/ai_chat/presentation/screens/chat_screen.dart"
      to: "lib/l10n/app_en.arb"
      via: "context.l10n.chat_action_wallet_not_found"
      pattern: "l10n\\.chat_action_wallet_not_found"
    - from: "lib/features/ai_chat/presentation/screens/chat_screen.dart"
      to: "lib/l10n/app_en.arb"
      via: "context.l10n.chat_subscription_suggest"
      pattern: "l10n\\.chat_subscription_suggest"
---

<objective>
Fix the 5 compile errors in chat_screen.dart caused by missing l10n keys, verify and fix AI language enforcement, verify AI date context injection, and expand transfer detection keywords in the system prompt.

Purpose: Unblock all other Phase 4 work by fixing compile errors, then polish the AI's language fidelity and transfer intent detection.
Output: Zero-error compilation, bilingual transfer keyword expansion in system prompt, consistent date injection.
</objective>

<execution_context>
@D:/Masarify-Plus/.claude/get-shit-done/workflows/execute-plan.md
@D:/Masarify-Plus/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-ai-voice-subscriptions-polish/04-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. -->

From lib/core/services/ai/chat_action_messages.dart:
```dart
class ChatActionMessages {
  const ChatActionMessages({
    // ... existing fields ...
    required this.walletNotFound,        // String Function(String name)
    required this.transferSameWallet,    // String
    required this.transferCreated,       // String Function(String amount, String from, String to)
  });
}
```

From lib/features/ai_chat/presentation/screens/chat_screen.dart (lines using missing keys):
```dart
// Line 69: context.l10n.recap_prime_message
// Line 224: l10n.chat_action_wallet_not_found (parameterized: String name)
// Line 225: l10n.chat_action_transfer_same_wallet (simple string)
// Line 232: l10n.chat_action_transfer_created (parameterized: String amount, String from, String to)
// Line 250: l10n.chat_subscription_suggest (parameterized: String title)
```

From lib/core/services/ai/ai_chat_service.dart:
```dart
// EN prompt: Line 207 — current transfer keywords: "حولت, سديت, نقلت, transferred"
// AR prompt: Line 283 — current transfer keywords: "حولت, سديت, نقلت, transferred"
// AR prompt: Line 277 — BUG: uses DateTime.now().month instead of ${now.month}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add missing l10n keys to both ARB files</name>
  <files>lib/l10n/app_en.arb, lib/l10n/app_ar.arb</files>
  <read_first>
    - lib/l10n/app_en.arb
    - lib/l10n/app_ar.arb
    - lib/features/ai_chat/presentation/screens/chat_screen.dart (lines 60-75 and 210-260)
    - lib/core/services/ai/chat_action_messages.dart
  </read_first>
  <action>
Add the following 5 missing l10n keys to `lib/l10n/app_en.arb`:

1. `"recap_prime_message"` — Simple string (no parameters). English: `"How was my spending today?"`. This is the auto-sent message when the daily recap notification opens the chat.

2. `"chat_action_wallet_not_found"` — Parameterized with `{name}` (String). English: `"Could not find account \"{name}\""`. Add the `@chat_action_wallet_not_found` metadata block with `placeholders: { "name": { "type": "String" } }`.

3. `"chat_action_transfer_same_wallet"` — Simple string. English: `"Source and destination accounts must be different"`.

4. `"chat_action_transfer_created"` — Parameterized with `{amount}`, `{from}`, `{to}` (all String). English: `"Transfer of {amount} from \"{from}\" to \"{to}\" created!"`. Add the `@chat_action_transfer_created` metadata block with all 3 placeholders.

5. `"chat_subscription_suggest"` — Parameterized with `{title}` (String). English: `"💡 \"{title}\" looks like a recurring payment. Add to Subscriptions & Bills?"`. Add the `@chat_subscription_suggest` metadata block.

Add the matching Arabic translations to `lib/l10n/app_ar.arb`:

1. `"recap_prime_message"`: `"إزاي كان صرفي النهارده؟"`
2. `"chat_action_wallet_not_found"`: `"مش لاقي حساب \"{name}\""` with metadata
3. `"chat_action_transfer_same_wallet"`: `"حساب المصدر والوجهة لازم يكونوا مختلفين"`
4. `"chat_action_transfer_created"`: `"تم تحويل {amount} من \"{from}\" لـ \"{to}\"!"` with metadata
5. `"chat_subscription_suggest"`: `"💡 \"{title}\" شكله دفعة متكررة. تضيفه للاشتراكات والفواتير؟"` with metadata

Place the keys in the appropriate section (near existing `chat_action_*` keys around lines 884-945 in the EN file). Follow exact ARB metadata format used by existing parameterized keys like `chat_action_category_not_found`.

After editing both files, run `flutter gen-l10n` to regenerate the localization classes.
  </action>
  <verify>
    <automated>cd D:/Masarify-Plus && flutter gen-l10n && flutter analyze lib/features/ai_chat/presentation/screens/chat_screen.dart</automated>
  </verify>
  <acceptance_criteria>
    - grep "recap_prime_message" lib/l10n/app_en.arb returns a match
    - grep "chat_action_wallet_not_found" lib/l10n/app_en.arb returns a match
    - grep "chat_action_transfer_same_wallet" lib/l10n/app_en.arb returns a match
    - grep "chat_action_transfer_created" lib/l10n/app_en.arb returns a match
    - grep "chat_subscription_suggest" lib/l10n/app_en.arb returns a match
    - grep "recap_prime_message" lib/l10n/app_ar.arb returns a match
    - grep "chat_action_wallet_not_found" lib/l10n/app_ar.arb returns a match
    - grep "chat_action_transfer_same_wallet" lib/l10n/app_ar.arb returns a match
    - grep "chat_action_transfer_created" lib/l10n/app_ar.arb returns a match
    - grep "chat_subscription_suggest" lib/l10n/app_ar.arb returns a match
    - flutter analyze lib/features/ai_chat/ shows "No issues found"
  </acceptance_criteria>
  <done>All 5 missing l10n keys exist in both app_en.arb and app_ar.arb with correct parameter metadata. chat_screen.dart compiles without errors. flutter gen-l10n succeeds.</done>
</task>

<task type="auto">
  <name>Task 2: Fix AR prompt date bug and expand transfer keywords in system prompt</name>
  <files>lib/core/services/ai/ai_chat_service.dart</files>
  <read_first>
    - lib/core/services/ai/ai_chat_service.dart (full file — focus on lines 100-285)
    - lib/core/services/ai/chat_action.dart (to see CreateTransferAction fields)
  </read_first>
  <action>
**Fix AI-02 (AR budget date bug):**
In `_buildArabicPrompt()` (around line 277), replace:
```dart
'create_budget: {"action":"create_budget","category":"Food","limit":3000,"month":${DateTime.now().month},"year":${DateTime.now().year}}\n'
```
with:
```dart
'create_budget: {"action":"create_budget","category":"Food","limit":3000,"month":${now.month},"year":${now.year}}\n'
```
NOTE: `now` is already a parameter of `_buildArabicPrompt` — but it is not currently accepted. The method signature must be updated to accept a `DateTime now` parameter. Looking at the caller (the `if (isAr)` branch around line 132), `now` is defined as `ctx.currentDate` on line 106. Pass `now: now` from the caller and add `required DateTime now` to the method signature. The EN prompt already uses `${now.month}` (line 201) via the `now` variable in the outer scope.

Wait — re-examine: The `_buildArabicPrompt` method does NOT receive `now`. But the outer method DOES have `now`. The fix requires either:
(a) Adding `now` as a parameter to `_buildArabicPrompt`, or
(b) Using `dateStr` which is already passed and extracting month/year from it.

The simplest approach: Add `required DateTime now` to the `_buildArabicPrompt` method signature, pass `now: now` from the caller, and use `${now.month}` and `${now.year}` in the AR budget example.

**Expand AI-06 (Transfer keywords):**

In the **English prompt** (around line 207), replace:
```
'TRANSFER DETECTION: When user mentions moving money between accounts (حولت, سديت, نقلت, transferred), use create_transfer. NEVER split a transfer into two transactions.\n'
```
with:
```
'TRANSFER DETECTION: When user mentions moving money between accounts, use create_transfer. Keywords: transferred, transfer, moved, sent, paid off, settled, cleared, from X to Y, حولت, سديت, نقلت, دفعت على, سددت, حولت من...لـ, نقلت فلوس, دفعت من X عشان Y, بعت فلوس. NEVER split a transfer into two transactions.\n'
```

In the **Arabic prompt** (around line 283), replace:
```
'كشف التحويلات: عند ذكر نقل فلوس بين حسابات (حولت, سديت, نقلت, transferred), استخدم create_transfer. لا تقسم التحويل إلى معاملتين أبداً.\n'
```
with:
```
'كشف التحويلات: عند ذكر نقل فلوس بين حسابات، استخدم create_transfer. كلمات: حولت, سديت, نقلت, سددت, دفعت على, حولت من...لـ, نقلت فلوس, بعت فلوس, دفعت من X عشان Y, transferred, transfer, moved, settled, paid off, from X to Y. لا تقسم التحويل إلى معاملتين أبداً.\n'
```

This adds 8 new transfer-intent keywords/phrases to each prompt (meeting the D-06 "5-8 more" requirement), including Egyptian conversational patterns like "دفعت من CIB عشان أسدد NBE" and "بعت فلوس".
  </action>
  <verify>
    <automated>cd D:/Masarify-Plus && flutter analyze lib/core/services/ai/ai_chat_service.dart</automated>
  </verify>
  <acceptance_criteria>
    - grep "سددت" lib/core/services/ai/ai_chat_service.dart returns matches in both EN and AR prompt sections
    - grep "paid off" lib/core/services/ai/ai_chat_service.dart returns a match
    - grep "بعت فلوس" lib/core/services/ai/ai_chat_service.dart returns a match
    - grep "دفعت من X عشان Y" lib/core/services/ai/ai_chat_service.dart returns a match
    - The string "DateTime.now().month" does NOT appear in _buildArabicPrompt (the AR date bug is fixed)
    - grep "now.month" lib/core/services/ai/ai_chat_service.dart returns matches in BOTH EN and AR prompt sections
    - flutter analyze lib/core/services/ai/ai_chat_service.dart shows "No issues found"
  </acceptance_criteria>
  <done>Arabic prompt uses ctx.currentDate for budget month/year (not DateTime.now()). Both EN and AR prompts contain 10+ transfer-intent keywords including Egyptian conversational patterns. Zero analyzer issues.</done>
</task>

</tasks>

<verification>
1. `flutter analyze lib/` — zero issues (no regression from l10n or prompt changes)
2. `flutter gen-l10n` — succeeds without warnings
3. All 5 previously-missing l10n keys resolve in both English and Arabic
4. Transfer keyword count in both prompts is >= 10 (was 4)
5. AR prompt uses `now.month`/`now.year` (not `DateTime.now().month`)
</verification>

<success_criteria>
1. `flutter analyze lib/` reports "No issues found" — the 5 compile errors in chat_screen.dart are resolved.
2. Both ARB files contain all 5 new keys with correct parameter metadata.
3. The Arabic system prompt uses the injected date context (not DateTime.now()) for budget examples.
4. Both EN and AR prompts contain >= 10 transfer-intent keywords including "سددت", "paid off", "بعت فلوس", "دفعت من X عشان Y".
</success_criteria>

<output>
After completion, create `.planning/phases/04-ai-voice-subscriptions-polish/04-01-SUMMARY.md`
</output>
