# Phase 3.1: Regression Fix & Feature Audit - Discussion Log (Auto Mode)

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions captured in CONTEXT.md — this log preserves the analysis.

**Date:** 2026-03-28
**Phase:** 03.1-regression-fix-feature-audit
**Mode:** auto (all decisions auto-selected with recommended defaults)
**Areas analyzed:** Runtime verification, Label naming, Cash wallet, Account management, Home screen RTL, Voice Confirm UX, Transaction form, SnackBar position, AI language, Brand icons, Subscriptions screen, Transaction CRUD

---

## Codebase Audit Results

A thorough codebase audit (Explore agent, very thorough) found that **all 18 reported regressions exist in source code**. The features are implemented but the user reports them as broken/missing when testing the app.

**Diagnosis:** The gap between "code exists" and "user can use it" suggests:
1. Entry points changed during Phase 3 home screen overhaul (features exist but can't be reached)
2. Runtime rendering bugs (layout, font sizes, duplicate elements)
3. Stale build (user may be testing pre-Phase-3 APK)
4. Labeling mismatch (user wants "Subscriptions & Bills", code has "Recurring & Bills")
5. Expectation gap (brand icons = initials, user expected logos)

---

## Auto-Selected Decisions

| Area | Decision | Rationale |
|------|----------|-----------|
| Verification approach | Build fresh APK, verify each feature on emulator | Code audit insufficient — must test at runtime |
| Label naming | "Subscriptions & Bills" everywhere | User's explicit request, repeated multiple times |
| Cash wallet | Verify visible in account chips | System wallet exists in DB, may be filtered from new UI |
| Account reorder/archive | Verify entry point from new home screen | Features exist in account_manage_sheet but may be unreachable |
| RTL alignment | Test in Arabic on emulator | Code uses DirectionalWidgets but rendering may differ |
| Voice Confirm | Fix layout bugs + add Accept All + multi-account suggest | User's explicit "less steps" requirement |
| Additional details | Evaluate toggle approach | User wants rethinking, not just verification |
| SnackBar position | Check margin values | Floating behavior correct but margins may be wrong |
| AI language | Verify Arabizi prompt + add code-level hint if needed | Prompt exists but may not work with current models |
| Brand icons | Verify rendering, defer actual logos | Initial-based approach is shipped design |
| Subscriptions screen | Verify against prior promises | May be correct or may need polish |
| Transaction CRUD | Verify at runtime first | May work correctly — verify before declaring broken |

---

## Claude's Discretion

- Accept All button placement for multi-draft
- Category auto-assign vs multi-select approach
- Additional details toggle implementation
- SnackBar margin adjustment values
- Arabizi detection strategy (prompt vs code-level)

## Deferred Ideas

- Actual brand logo assets (licensing + bundling needed)
- Full CRUD screen UI overhaul (if they work, it's not a regression)

---

*Discussion log for: 03.1-regression-fix-feature-audit*
*Date: 2026-03-28*
