---
phase: "03.1"
plan_number: 3
title: "AI Language Verification, Brand Icons, Full Regression Pass"
wave: 2
depends_on: ["03.1-01", "03.1-02"]
files_modified:
  - lib/core/services/ai/ai_chat_service.dart
  - lib/core/constants/brand_registry.dart
  - lib/features/recurring/presentation/screens/recurring_screen.dart
  - lib/features/transactions/presentation/screens/transaction_detail_screen.dart
  - lib/features/dashboard/presentation/widgets/account_manage_sheet.dart
autonomous: true
---

# Plan 03.1-03: AI Language Verification, Brand Icons, Full Regression Pass

<objective>
Verify AI language detection (Arabizi, Arabic, English) works correctly in the system prompt, verify brand icon rendering with the colored-initial approach, and perform a full regression verification pass across all 26 decisions to confirm that Plans 01 and 02 integrated correctly and remaining features work as expected.
</objective>

<must_haves>
- AI system prompt correctly instructs Arabizi input to get Arabic responses
- Brand icon registry matches transactions and renders colored initials
- Subscriptions & Bills screen renders all sections (Overdue, Upcoming, Active, Paid, Paused)
- Transaction detail screen shows correct data with working edit/delete actions
- Account management features (reorder, archive, default) are functional
- All D-01 through D-26 verified or fixed
</must_haves>

## Tasks

<task id="1">
<title>Verify AI Arabizi detection in system prompt (D-20, D-21, D-22)</title>
<read_first>
- lib/core/services/ai/ai_chat_service.dart
</read_first>
<action>
Verify the system prompt in `ai_chat_service.dart` contains Arabizi detection instructions.

**Current state (line 155-156):**
The English system prompt already contains:
```
EXCEPTION: If user writes in Franco-Arab/Arabezi (Arabic in Latin letters, e.g. "3ayz", "7aga", "el", "kda", "ana"), ALWAYS respond in Arabic.
```

**Current state (line 232-233):**
The Arabic system prompt also contains:
```
EXCEPTION: If user writes in Franco-Arab/Arabezi (Arabic in Latin letters, e.g. "3ayz", "7aga", "el", "kda", "ana"), ALWAYS respond in Arabic.
```

This covers D-20 (Arabizi instruction exists) and D-21 (same-language response). The prompt already:
1. Forces responses in the user's locale language (line 155: "You MUST respond in {langName}")
2. Has the Arabizi exception (line 156: Franco-Arab triggers Arabic response)

**For D-22:** Check if additional code-level Arabizi detection is needed. The system prompt approach may not be reliable with all models. Add an Arabizi pattern detector that sets a language hint.

In `ai_chat_service.dart`, before the `chat()` method sends the message, add a helper:

```dart
/// Detects Franco-Arab/Arabizi patterns in user text.
/// Returns true if text likely contains Arabizi (Arabic in Latin letters).
static bool _isArabizi(String text) {
  // Numbers-for-letters: 3=ع, 7=ح, 2=ء, 5=خ, 8=ق, 9=ص
  final arabiziNumbers = RegExp(r'[237589](?=[a-zA-Z])|(?<=[a-zA-Z])[237589]');
  // Common Arabizi words
  const arabiziWords = [
    'ana', 'enta', 'enty', 'mesh', 'msh', 'kda', 'keda', 'leih', 'leh',
    'ezzay', 'ezay', 'aywa', 'la2', 'tab', 'yalla', 'bas', 'khalas',
    'habibi', 'shokran', 'ahlan', 'gneih', 'geneh', 'sraft', 'dafa3t',
    '3ayz', '3ayza', '7aga', 'fe', 'fi', 'el', 'wel', 'betaa',
  ];

  final lower = text.toLowerCase();
  if (arabiziNumbers.hasMatch(lower)) return true;
  final words = lower.split(RegExp(r'\s+'));
  int matches = 0;
  for (final w in words) {
    if (arabiziWords.contains(w)) matches++;
  }
  // If 2+ Arabizi words detected, classify as Arabizi
  return matches >= 2;
}
```

Then, in the system prompt builder, if `_isArabizi(userMessage)` is true and the current locale is English, append an additional instruction:
```
\nIMPORTANT: The user's message is in Arabizi (Arabic using Latin letters). You MUST reply in Arabic.
```

This provides a code-level assist on top of the prompt-level instruction, making the detection more reliable across models.
</action>
<acceptance_criteria>
- grep -q "Franco-Arab" lib/core/services/ai/ai_chat_service.dart
- grep -q "_isArabizi\|isArabizi" lib/core/services/ai/ai_chat_service.dart
- The system prompt contains Arabizi exception instructions for both English and Arabic modes
</acceptance_criteria>
</task>

<task id="2">
<title>Verify brand icon rendering at code level (D-23, D-24)</title>
<read_first>
- lib/core/constants/brand_registry.dart
- lib/core/utils/category_icon_mapper.dart
</read_first>
<action>
Per D-23, verify brand icon rendering works at the code level. Per D-24, actual logo assets are out of scope — the colored-initial approach is the shipped design.

**Verify BrandRegistry:**
1. Check that `BrandRegistry.brands` list contains 40+ entries with keywords, colors, and initials.
2. Check that there is a lookup method that matches transaction titles against brand keywords.
3. Verify ordering: more specific brands (e.g., "Vodafone Cash") come before less specific ones (e.g., "Vodafone").

**Check if BrandRegistry is wired to TransactionCard:**
Search for `BrandRegistry` usage in the transaction card rendering pipeline:
```
grep -r "BrandRegistry\|brand_registry" lib/shared/widgets/cards/ lib/features/dashboard/ lib/features/transactions/
```

If `BrandRegistry` is referenced in `TransactionCard` or its helper widgets, the brand icons should render. If NOT referenced, the brand icon feature exists in code but was never wired to the transaction display — it would need to be connected.

Common wiring pattern:
- TransactionCard renders a leading icon
- That icon should check `BrandRegistry.match(transaction.title)` and if a match is found, show a colored circle with the brand initial instead of the category icon

If the wiring is missing, add it to the transaction card's leading icon builder. If the wiring exists, mark as "verified — no fix needed".

No changes to the brand list itself. The colored-initial approach is the correct shipped design per D-24.
</action>
<acceptance_criteria>
- grep -q "BrandRegistry\|BrandInfo" lib/shared/widgets/cards/transaction_card.dart OR confirm brand icon wiring exists in the rendering pipeline
- Brand registry contains 40+ brand entries (verify by counting entries in brand_registry.dart)
- Vodafone Cash appears before Vodafone in the list (more specific first)
</acceptance_criteria>
</task>

<task id="3">
<title>Verify Subscriptions & Bills screen renders correctly (D-25)</title>
<read_first>
- lib/features/recurring/presentation/screens/recurring_screen.dart
</read_first>
<action>
Per D-25, verify the Subscriptions & Bills screen renders all sections. Read the full `recurring_screen.dart` to confirm:

1. The screen title uses `context.l10n.recurring_and_bills_title` (which now resolves to "Subscriptions & Bills" after Plan 01 changes).
2. The screen organizes rules into sections: Overdue, Upcoming Bills, Active, Paid/Completed, Paused.
3. Each section renders correctly with the appropriate filtering logic.
4. The "Add" button in the app bar navigates to `AppRoutes.recurringAdd`.

If the screen renders all sections correctly at the code level (correct filtering, section headers, empty states), mark as "verified — no fix needed".

If any section is missing (e.g., no "Paused" section, or "Overdue" section not filtering by date), add the missing section using the same pattern as existing sections.

Also verify that the screen title is not hardcoded — it should use the l10n key which now contains "Subscriptions & Bills".
</action>
<acceptance_criteria>
- grep -q "context.l10n.recurring_and_bills_title" lib/features/recurring/presentation/screens/recurring_screen.dart
- The screen uses the l10n key (not a hardcoded string) for its title
- Sections for different rule states are present in the build method
</acceptance_criteria>
</task>

<task id="4">
<title>Verify transaction detail screen and CRUD (D-26)</title>
<read_first>
- lib/features/transactions/presentation/screens/transaction_detail_screen.dart
</read_first>
<action>
Per D-26, verify the transaction detail screen:

1. Shows correct data: amount, category, account, date, note, type.
2. Edit action: navigates to edit mode of AddTransactionScreen with the transaction ID.
3. Delete action: shows confirmation dialog, then deletes.

Read `transaction_detail_screen.dart` fully and verify:
- It watches `transactionByIdProvider(id)` for reactive data.
- It displays all relevant fields from `TransactionEntity`.
- Edit button navigates to `/transactions/{id}/edit`.
- Delete button shows a confirmation dialog.
- Null/not-found states are handled with `EmptyState`.

If all of the above exist in the code, mark as "verified — no fix needed". The screen was reported as "untouched" by the user, but it may be functional as-is — the concern was about CRUD screens not being overhauled, which is deferred to a future phase per the CONTEXT.md deferred section.
</action>
<acceptance_criteria>
- grep -q "transactionByIdProvider" lib/features/transactions/presentation/screens/transaction_detail_screen.dart
- grep -q "edit\|Edit" lib/features/transactions/presentation/screens/transaction_detail_screen.dart
- grep -q "delete\|Delete" lib/features/transactions/presentation/screens/transaction_detail_screen.dart
- The screen handles loading, error, and null states
</acceptance_criteria>
</task>

<task id="5">
<title>Verify account management features work at code level (D-08 to D-11)</title>
<read_first>
- lib/features/dashboard/presentation/widgets/account_manage_sheet.dart
</read_first>
<action>
Per D-08 through D-11, the AccountManageSheet must support reorder, archive, unarchive, and set-as-default. Plan 01 Task 7 adds the entry point. This task verifies the sheet itself works.

Read `account_manage_sheet.dart` fully and verify:

1. **Reorder (D-09):** `_onReorder` method exists, uses `ReorderableListView` or equivalent. The method calls `walletRepositoryProvider` to persist sort order changes.

2. **Archive (D-10):** There is an archive action (icon button or swipe) that calls `walletRepository.update(...)` with `isArchived: true`. Verify a confirmation dialog is shown before archiving.

3. **Unarchive (D-10):** Archived accounts are visible in the sheet (with visual distinction like strikethrough or opacity). An unarchive action restores `isArchived: false`.

4. **Default account (D-11):** There is a "set as default" action. The default account is visually distinguished (e.g., star icon, bold label, or "Default" badge). Default account cannot be deleted.

If all four features exist in the code, mark as "verified — no fix needed". If any feature is missing, flag it for implementation.

Key things to check:
- `_onReorder` calls the repository to persist new sort order
- Archive/unarchive toggles `isArchived` flag via repository
- Default account has a "set as default" UI action
- The sheet loads ALL wallets including archived (line 42: `getAllIncludingArchived()`)
</action>
<acceptance_criteria>
- grep -q "_onReorder\|onReorder" lib/features/dashboard/presentation/widgets/account_manage_sheet.dart
- grep -q "isArchived\|archive" lib/features/dashboard/presentation/widgets/account_manage_sheet.dart
- grep -q "isDefaultAccount\|setDefault\|default" lib/features/dashboard/presentation/widgets/account_manage_sheet.dart
- The sheet loads all wallets including archived via getAllIncludingArchived()
</acceptance_criteria>
</task>

<task id="6">
<title>Verify Cash wallet resolution in AI voice/chat (D-07)</title>
<read_first>
- lib/core/utils/wallet_resolver.dart
- lib/core/services/ai/ai_chat_service.dart
</read_first>
<action>
Per D-07, verify the AI voice/chat correctly resolves "cash", "كاش", and Arabizi variants to the system Cash wallet.

Check `wallet_resolver.dart` (if it exists) or the wallet matching logic in `voice_confirm_screen.dart`:
1. Does the matching logic handle "cash" (English)?
2. Does it handle "كاش" (Arabic)?
3. Does it handle system wallets? The current `_applyDefaults` in voice_confirm_screen.dart (line 97) filters `wallets.where((w) => !w.isSystemWallet)` for the wallet list, which EXCLUDES the Cash wallet from matching.

If `_applyDefaults` excludes system wallets from the wallet matching pool:
- The Cash wallet will NEVER match, even if the user says "cash".
- This is a bug. Fix by including the system wallet in the matching pool but keeping it excluded from the default fallback.

In `voice_confirm_screen.dart`, around line 97-100, change:
```dart
final nonSystem = wallets.where((w) => !w.isSystemWallet).toList();
```

The wallet hint matching block (line 135-172) should search ALL wallets (including system) for matches:
```dart
// Use all wallets for hint matching (includes system Cash wallet)
// Only fall back to non-system default if no match found
```

Change the hint matching section to search `wallets` (all) instead of limiting to `nonSystem`. Keep the default fallback (line 173) using `defaultAccountId` from `nonSystem`.

This way:
- "cash" / "كاش" → matches system Cash wallet
- "CIB" → matches user's CIB account
- No match → falls back to default non-system account

Also verify the AI chat service's wallet list includes the Cash wallet in the `FinancialContext.walletList` — check if system wallets are excluded from the wallet context passed to the AI prompt.
</action>
<acceptance_criteria>
- The wallet hint matching in _applyDefaults searches ALL wallets (including system Cash wallet)
- grep -q "wallets.where" in the matching section confirms the broader search
- The default fallback still uses nonSystem wallets (not the system Cash wallet)
</acceptance_criteria>
</task>

<task id="7">
<title>Full regression verification checklist (D-01 to D-03)</title>
<read_first>
- lib/features/dashboard/presentation/screens/dashboard_screen.dart
- lib/features/dashboard/presentation/widgets/balance_header.dart
- lib/features/voice_input/presentation/screens/voice_confirm_screen.dart
- lib/features/recurring/presentation/screens/recurring_screen.dart
</read_first>
<action>
This is the final verification pass per D-01, D-02, D-03. Run `flutter analyze lib/` and ensure zero issues.

Create a runtime verification checklist (for manual testing on emulator):

**Label & Navigation:**
- [ ] Nav tab shows "Subscriptions" (not "Recurring")
- [ ] Subscriptions & Bills screen title shows "Subscriptions & Bills"
- [ ] Hub section shows "Subscriptions & Bills"

**Home Screen:**
- [ ] Cash wallet visible in account chips
- [ ] Gear icon visible next to account chips
- [ ] Tapping gear opens AccountManageSheet
- [ ] Balance header renders in both LTR and RTL

**SnackBar:**
- [ ] Success/error SnackBars appear above nav bar, not mid-screen

**Voice Confirm Screen:**
- [ ] Category auto-assigned from voice parsing
- [ ] Transfer shows From and To account fields
- [ ] Unmatched wallet shows "Create" hint
- [ ] Multi-draft shows "Save All" button
- [ ] Amount uses design token text styles (no oversized fonts)

**Account Management:**
- [ ] Reorder via drag-and-drop works
- [ ] Archive hides account from chips
- [ ] Unarchive restores visibility
- [ ] Default account is visually marked

**Transaction Detail:**
- [ ] Shows correct data (amount, category, account, date)
- [ ] Edit navigates to edit form
- [ ] Delete shows confirmation

**AI Chat:**
- [ ] English input gets English response
- [ ] Arabic input gets Arabic response
- [ ] Arabizi input gets Arabic response

**Brand Icons:**
- [ ] Transaction card for "Vodafone" shows colored initial "V" or "VC"

Run `flutter analyze lib/` as the final gate. All issues must be zero.
</action>
<acceptance_criteria>
- flutter analyze lib/ returns "No issues found!"
- No compilation errors
- All modified files use design tokens (no hardcoded colors, sizes, or strings)
</acceptance_criteria>
</task>

## Verification

- [ ] `flutter analyze lib/` reports 0 issues
- [ ] AI system prompt contains Arabizi detection instructions (both English and Arabic modes)
- [ ] Arabizi code-level detector function exists in ai_chat_service.dart
- [ ] Brand registry has 40+ brands, Vodafone Cash before Vodafone
- [ ] Subscriptions & Bills screen uses l10n key for title
- [ ] Transaction detail screen has edit/delete actions
- [ ] Account manage sheet has reorder, archive, and default features
- [ ] Cash wallet included in voice matching pool (not filtered by isSystemWallet)
- [ ] Full regression checklist documented for manual runtime testing
