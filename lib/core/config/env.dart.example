/// API keys for external services.
///
/// Copy this file to `env.dart` and fill in your own keys.
/// NEVER commit `env.dart` to version control — it is .gitignored.
abstract final class Env {
  static const String openRouterApiKey = '';
}
